def fact(n)
    if n < 0
        return -1
    elif n >= 0 and n < 2
        return 1
    else
        return n * fact(n-1)
    end
end

def fact2(n)
    prod := 1
    while n > 0
        prod = prod * n
        n = n - 1
    end
    return prod
end

print(fact(10))
print(fact2(10))
